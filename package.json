{  
  "name": "nexusct-website",  
  "version": "1.0.0",  
  "description": "An Eleventy-based static site with hybrid backend integration.",  
  "scripts": {  
    "start": "eleventy --serve",  
    "build": "eleventy"  
  },  
  "devDependencies": {  
    "@11ty/eleventy": "^1.0.0"  
  }  
}